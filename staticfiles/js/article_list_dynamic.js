// JS for article list page (if needed)
document.addEventListener('DOMContentLoaded', function() {
    // Example: focus search input if present
    var search = document.querySelector('.article-search-input');
    if(search) search.focus();
    // Add more JS as needed
});
